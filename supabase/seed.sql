-- Seed data is intentionally user-scoped and therefore created after sign-in.
-- In the iOS App, use “设置 → 导入演示词库” to insert a realistic starter set
-- through the same authenticated API used in production.

