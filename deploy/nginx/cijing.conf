server {
    listen 80;
    listen [::]:80;
    server_name cijing.joy-coder.com;

    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name cijing.joy-coder.com;

    root /var/www/cijing;
    index index.html;
    server_tokens off;

    ssl_certificate /etc/nginx/ssl/cijing/fullchain.crt;
    ssl_certificate_key /etc/nginx/ssl/cijing/private.key;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_session_cache shared:CIJING_SSL:10m;
    ssl_session_timeout 1d;

    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "DENY" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    location = / {
        try_files /index.html =404;
    }

    # Prefer directory index files so /privacy returns 200, not a slash redirect.
    location / {
        try_files $uri $uri/index.html $uri.html =404;
    }
}
